using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using Android.App;
using Android.Content;
using Android.OS;
using Android.Runtime;
using Android.Views;
using Android.Widget;

namespace BallDetector.Server
{
    public static class ServerComs
    {
        // temp function to request a name from the server
        // to check if its the same as the registered one
        public static bool RequestName(string name)
        {
            // if name is not on the server
            return true;
        }
    }
}